export const users = [
  {
    id: 101,
    firstName: "mukesh",
    lastName: "kumar",
    email: "mukesh@mail.com",
    password: "muk123",
  },
  {
    id: 102,
    firstName: "suresh",
    lastName: "gupta",
    email: "suresh@mail.com",
    password: "sur123",
  },
  {
    id: 103,
    firstName: "geeta",
    lastName: "pal",
    email: "geeta@mail.com",
    password: "geet123",
  },
];
/* export const quotes = [
    { name: "what a great day", by: "101" },
    { name: "have a awesome day", by: "102" },
    { name: "morning day starts with coffee", by: "101" }
];
 */
---------------

import { ApolloServer, gql } from "apollo-server";
import { ApolloServerPluginLandingPageGraphQLPlayground } from "apollo-server-core";
import { users } from "./userdb.js";
const typeDefs = gql`
  type Query {
    users: [User]
  }
  type User {
    id: ID
    firstName: String
    lastName: String
    email: String
  }
`;
const resolvers = {
  Query: {
    users: () => users,
  },
};
const server = new ApolloServer({
  typeDefs,
  resolvers,
  plugins: [ApolloServerPluginLandingPageGraphQLPlayground()],
});

server.listen().then(({ url }) => {
  console.log(`server is ready ${url}`);
});
